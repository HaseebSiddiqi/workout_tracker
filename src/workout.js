const workoutName = prompt("Enter Workout Name");
const workoutDate = prompt("Enter Workout Date");

const workout ={
    name: workoutName,
    date: workoutDate,
    exercises: [exercise],
};

const exercise ={
    name: "",
    weight: "",
    reps: [0,0,0]
};



console.log(workout);
